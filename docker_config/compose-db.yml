version: "3.0"

services:

  pg:
    image: postgres:latest
    environment:
      - POSTGRES_DB=swp_database
      - POSTGRES_USER=ksnofrubegerkn
      - POSTGRES_PASSWORD=PIbIBupIBPIubKJDNMLMwemlwO
      - PGDATA=/var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    expose:
      - "5432"
